diff --git a/src/multi.ts b/src/multi.ts
--- a/src/multi.ts
+++ b/src/multi.ts
@@ -1,3 +1,4 @@
 export const one = 1;
-export const two = 2;
+export const two = one + 1;
+export const three = two + 1;
 export const done = true;
@@ -10,2 +11,3 @@ export function run() {
 console.log(one);
-console.log(two);
+console.log(two);
+console.log(three);
