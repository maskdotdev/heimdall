# UI Package

Placeholder for shared UI primitives used by frontend apps.

Keep this package focused on reusable presentation components. Product workflows should remain in app-level `features/` directories.

