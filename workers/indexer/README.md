# Indexer Worker

The indexer is optional and should be added only when the default code intelligence worker and native tools are not enough.

Expected future ownership:

- Large repository indexing.
- Fast symbol extraction.
- Fast dependency graph construction.
- High-throughput source scanning.
- Tree-sitter hot paths.
