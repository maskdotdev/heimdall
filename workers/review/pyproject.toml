[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "heimdall-review-worker"
version = "0.0.0"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
