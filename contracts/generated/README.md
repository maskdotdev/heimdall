# Generated Contracts

These files are generated from `contracts/schemas`.

Do not edit generated runtime types by hand. Update the source schema, then run:

```sh
pnpm contracts:generate
```

Use `pnpm contracts:check` to validate schemas, fixtures, and generation drift before handoff.
